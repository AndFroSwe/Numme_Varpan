% Print nice Hermite curve between 2 points

points = 100;
h = 0.005;
u = start_vec(45, 19);
bana_big = RKode(u, h*2);
bana_small = RKode(u, h);

v = [];
for i = 1:points
    x1 = bana_big.x(1);
    x2 = bana_big.x(2);
    p = Hermite((x2-x1)*i/
    
